# Parliament Fuel Coupon System - cPanel Requirements (Python 3.6 Compatible)
# Optimized for shared hosting with Python 3.6

# Core Django dependencies (Python 3.6 compatible)
Django==3.2.25
djangorestframework==3.14.0
djangorestframework-simplejwt==5.2.2
django-cors-headers==4.0.0

# Database (MySQL for cPanel hosting)
mysqlclient==2.1.1

dj-database-url==1.3.0

# Authentication and security (Python 3.6 compatible)
PyJWT==2.6.0
cryptography==3.4.8
requests==2.28.2
requests-oauthlib==1.3.1

# Business Central integration (Python 3.6 compatible)
msal==1.20.0

# Utilities (Python 3.6 compatible)
python-decouple==3.6
python-dotenv==0.21.1
Pillow==9.5.0

# Excel/CSV export (Python 3.6 compatible)
openpyxl==3.0.10

# PDF generation (Python 3.6 compatible)
reportlab==3.6.13

# QR Code generation (Python 3.6 compatible)
qrcode==7.3.1

# Production server (Python 3.6 compatible)
gunicorn==20.1.0
whitenoise==6.5.0

# Additional dependencies for Python 3.6
pytz==2022.7.1
sqlparse==0.4.4
asgiref==3.4.1
setuptools>=40.0.0

# Optional: API documentation (Python 3.6 compatible)
drf-spectacular==0.26.5

# Optional: Development tools (Python 3.6 compatible)
django-extensions==3.1.5
