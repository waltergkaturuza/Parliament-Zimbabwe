# Parliament Fuel Coupon System - cPanel Requirements (Python 3.6 Compatible)
# Using version ranges to ensure compatibility with available packages

# Core Django dependencies (Python 3.6 compatible ranges)
Django>=3.2,<4.0
djangorestframework>=3.12,<3.15
djangorestframework-simplejwt>=4.6,<5.3
django-cors-headers>=3.8,<4.1

# Database (MySQL for cPanel hosting)
mysqlclient>=2.0,<2.2

dj-database-url>=1.0,<2.0

# Authentication and security (Python 3.6 compatible ranges)
PyJWT>=2.0,<2.7
cryptography>=3.0,<4.0
requests>=2.25,<2.29
requests-oauthlib>=1.3,<1.4

# Business Central integration (Python 3.6 compatible)
msal>=1.15,<1.21

# Utilities (Python 3.6 compatible ranges)
python-decouple>=3.4,<3.7
python-dotenv>=0.19,<0.22
Pillow>=8.0,<10.0

# Excel/CSV export (Python 3.6 compatible)
openpyxl>=3.0,<3.1

# PDF generation (Python 3.6 compatible)
reportlab>=3.5,<4.0

# QR Code generation (Python 3.6 compatible)
qrcode>=6.1,<7.4

# Production server (Python 3.6 compatible)
gunicorn>=20.0,<21.0
whitenoise>=5.0,<7.0

# Basic dependencies
pytz
sqlparse>=0.4,<0.5
asgiref>=3.3,<3.5
setuptools>=40.0.0

# Optional: API documentation (if available)
drf-spectacular>=0.20,<0.27

# Optional: Development tools (if available)
django-extensions>=3.1,<3.2
